alias back='cd - > /dev/null'
alias cd..='cd ..'
alias prettyjson='python -m json.tool'
alias prettyxml='xmllint --format -'
alias path='echo -e ${PATH//:/\\n}'
alias svnnew='svn status | grep ^?'
alias dockervols='cd /home/trent/.local/share/containers/storage/volumes/'
# pkgfiles - suffix package name at the end
# e.g. `pkgfiles git-credential-libsecret``
alias pkgfiles='dnf repoquery --installed -l'
alias apexes='find db/apex -name "install.sql" | sed -E "s/^/@@/g" > .apexinstall.sql && echo "exit" >> .apexinstall.sql'
alias httpserve='python -m http.server'
alias sql='sql -L'
alias liquibase='liquibase --show-banner=false'
